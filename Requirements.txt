Requirements

Install dependencies:

pip install opencv-python pdf2image python-docx streamlit matplotlib

Install Poppler (Required for PDF)
Windows users download Poppler from:

https://github.com/oschwartz10612/poppler-windows/releases/
https://youtu.be/PyF1Vh9040Y?si=qiUh9zpQGUZ5z6ha

Then extract and set your path, e.g.:

makefile
Copy code
C:\poppler-25.11.0\Library\bin


🧠 How It Works (Pipeline Overview)
1. Load Invoice as Image
PDF → converted using pdf2image.convert_from_path

DOCX → rendered into blank white OpenCV image with text overlay

Output is a BGR image for OpenCV

2. Detect Text Blocks
Convert to grayscale

Binary threshold

Dilate text regions using a rectangular kernel

Find contours → extract bounding rectangles

3. Categorize Regions
Based on vertical position (y value):

Top 25% → Header

Middle 50% → Table

Bottom 25% → Totals

4. Draw Bounding Boxes
Blue → Header

Green → Table

Red → Totals

5. Streamlit UI
User uploads a PDF/DOCX → processed → displayed in browser.

🎨 Color Code
Section	Color
Header fields	Blue (255, 0, 0)
Table / Line Items	Green (0, 255, 0)
Totals Section	Red (0, 0, 255)

🗂 Sample Output
The processed invoice will show bounding boxes like:

Copy code
+----------------------------------------------------------+
| [BLUE BOX] Vendor Name                                   |
| [BLUE BOX] Invoice No | Date                             |
|                                                          |
| [GREEN BOX] Line Item Table                              |
|                                                          |
| [RED BOX] Totals                                         |
+----------------------------------------------------------+
